# Personal Claude Code instructions (private)
CLAUDE.local.md

# Private docs & reference material (planning, design, employer-specific — never committed)
private/
interview-prep/

# Secrets / env
.env
.env.*
*.pem

# Java / Maven
target/
build/
*.class
HELP.md

# Local raw event log (S3 stand-in) — runtime data, regenerated on each run, never committed
**/data/rawlog/
*.ndjson

# Node / CDK
node_modules/
cdk.out/

# IDE / OS
.idea/
.DS_Store
