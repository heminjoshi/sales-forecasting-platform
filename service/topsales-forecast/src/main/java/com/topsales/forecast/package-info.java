/**
 * Forecasting engine: {@code Forecaster} implementations (seasonal-naive, Holt-Winters),
 * the batch runner that writes versioned serving rows, and time-series-CV evaluation (WAPE + bias).
 *
 * <p>Populated in Phase 3.
 */
package com.topsales.forecast;
