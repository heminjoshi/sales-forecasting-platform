/**
 * Shared domain model, DTOs, configuration, and the interface seams
 * ({@code Forecaster}, {@code ForecastProvider}, {@code InsightGenerator}, repository ports)
 * that let local and AWS implementations swap behind the same contracts.
 *
 * <p>Populated in Phase 2 (domain model) and beyond.
 */
package com.topsales.common;
